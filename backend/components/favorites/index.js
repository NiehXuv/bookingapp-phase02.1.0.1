const { addToFavorites } = require("./addToFavorites");
const { removeFromFavorites } = require("./removeFromFavorites");
const { getFavorites } = require("./getFavorites");
const { clearFavorites } = require("./clearFavorites");

module.exports = {
  addToFavorites,
  removeFromFavorites,
  getFavorites,
  clearFavorites
};
