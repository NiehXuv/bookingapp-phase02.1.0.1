const { createSearchHistory } = require("./createSearchHistory");
const { getSearchHistory } = require("./getSearchHistory");
const { deleteSearchHistory } = require("./deleteSearchHistory");
const { clearSearchHistory } = require("./clearSearchHistory");

module.exports = {
  createSearchHistory,
  getSearchHistory,
  deleteSearchHistory,
  clearSearchHistory
};
